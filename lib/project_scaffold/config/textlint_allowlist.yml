# =============================================================
#  📝 Textlint 除外リスト - Vivlio Starter
# -------------------------------------------------------------
#  ここに書いた語は textlint の検査対象から外れます。
#  「この語はこのままでよい」と決めたものを並べてください。
#  正規表現も使えます（下の「言い換えの提案を止めたい語」に例があります）。
#
#  書くのは「この本の語」だけで構いません。どの本でも誤って指摘される類
#  （半角の `50%` など、校正ルール側の取りこぼし）は vs lint が予め打ち消します。
#
#  ※ 今の設定では指摘されない語も、書き方の見本として残してあります。
#     漢字の連続は 11 文字以上で指摘されます（config/.textlintrc.yml の
#     max-kanji-continuous-len）。
# =============================================================

# ------- 書籍名（漢字の連続チェックを外す） -------
- "現代計算機科学"
- "プログラミング言語理論"
- "計算機科学入門"

# ------- 資格名称（同上） -------
# 長い資格名はここへ。「第一級総合無線通信士」「第一級陸上無線技術士」のような名称も同じように書けます
- "情報処理安全確保支援士" # 漢字 11 文字。現在の上限でも指摘されるため必須
- "基本情報技術者"
- "応用情報技術者"
- "エンベデッドスペシャリスト"
- "データベーススペシャリスト"
- "ネットワークスペシャリスト"

# ------- 言い換えの提案を止めたい語 -------
# 「A => B」と直すよう促されるが、この本ではそのまま使うと決めた語を書きます
- "以下の" # 「以下の => 次の」を止める
- "以下に" # 「以下に => 次に」を止める
- "下記の" # 「下記の => 次の」を止める
- "既定" # 「既定 => デフォルト」を止める。この本は和語の「既定」で通す
- "/(プロジェクト|プロダクト)マネージャ/" # 正規表現の例。末尾長音（=> マネージャー）を止める
- "/マスター/"  # 「マスター => マスタ」を止める。

# ------- 語の一部が別の語に見えるとき -------
# 日本語の複合語の継ぎ目に辞書の見出し語と同じ並びが偶然現れ、
# 別の語として指摘されることがあります（「プレビューアプリ」の中の「レビューア」が校閲者と読まれる、など）。
# 語を一つずつ足すと追いつかないので、正規表現で「継ぎ目の形」を書いてください
#   - "/自社サービスアカウント/"

# ------- ここから下に追記してください -------
