# 既知の不具合（Known Issues）

> 💡 **運用のルール**
> 解消済みの不具合は本ファイルから削除し、`CHANGELOG.md` の `Fixed` に集約する。
> ここには**未解消の項目（既知の制限・将来対応・要確認・進行中の不具合）のみ**を残す。

---

## 進行中の不具合（要修正）

### オプションを位置引数より前に置けない
* **詳細:** `vs build --no-clean x.md` / `vs build --high 10` は 🔴「Could not parse token」になる。
  空白区切り・`=` 区切りとも、**オプションは位置引数より後**に置く必要がある
  （`vs build x.md --no-clean` は正常）。
* **実害の例:** `pdf:pages` / `pdf:rasterize` の入力 PDF は「省略時はビルド生成物」と案内しているが、
  省略するとオプションが先頭に来るため **`vs pdf:pages --dpi=200` が通らない**（`--dpi 200` と
  空白で区切っても同じ）。入力 PDF を明示すれば両記法とも通る。
* **原因:** Samovar の `Many#parse`（既定 `stop: /^-/`）が「`-` で始まるトークンに当たるまで」を
  貪欲に取り、`Command` は宣言順（`many` → `options`）に解析するため。全オプション共通の既存挙動で、
  `--opt=value` 共通化（2026-07-27）で解消したのは `=` 記法の対応差だけ。
* **対応方針:** `OptionTokenNormalizer` は「どのオプションが値を取るか」を options 定義から
  自動導出しているので、**オプション群を末尾へ寄せる**処理を足せば通せる（従来エラーだった入力が
  通るようになるだけで後方互換は壊れない）。ただし全コマンドの受理範囲が変わるため別タスクとする。
  背景 → `docs/specs/cli-option-parsing-report.md` §3.3。

### `Common.current_log_level` が ARGV を直接走査している
* **詳細:** ログ出力の閾値だけは `common.rb` が `ARGV` をパターンマッチで独自解釈しており
  （`/^--log=(.+)$/` と `['--log', level]` の 2 形）、正規化済みトークンと二重管理になっている。
* **影響:** 実害は出ていないが、**Samovar が弾いた入力でもログレベルだけは効く**という捻れが残る。
* **対応方針:** 正規化済みトークンから 1 回だけ決める形へ寄せられないか検討する。Samovar を
  通らない経路（起動直後のエラー表示など）からも呼ばれるため、呼び出し経路の洗い出しが要る。
  背景 → `docs/specs/cli-option-parsing-report.md` §4.3 ③。

---

## 既知の制限（運用回避推奨）

### Kindle でテキスト化できない複雑なインライン数式のサイズが不安定
* **詳細:** 2026-07-19 に **Kindle 限定のインライン数式テキスト化**を実装し、単純な式（`E=mc^2`・`1/299,792,458`・単位・ギリシャ・上下付き・記号など）は SVG 画像でなく HTML テキスト（`<i>`/`<sup>`/`<sub>`＋Unicode）へ変換されるようになった。テキストは定義上フォントサイズに 100% 追従するため、本文・表セル内とも崩れない（原稿 `contents/` の現行インライン数式はすべてこのサブセットに収まりテキスト化される）。
* **残る制限:** テキストで表現できない複雑な式（`\sqrt`・`\sum`・積分・2 段入れ子の上下付き・入れ子分数など）は従来どおり SVG 画像のまま残り、`px` 固定フォールバックが効くため巨大化はしないが、読者のフォント拡大には追従しない。
* **対応方針:** 複雑な式は**ディスプレイ数式（`$$…$$`）として行を占有させる**か、表セル内に置かないことを推奨（ディスプレイ数式は px 固定でも破綻しない）。詳細は `docs/specs/kindle-inline-math-textify-spec.md`。

---

## 要確認（軽微・検証待ち）

### EPUB の表紙画像が一回り小さく見える
* **詳細:** 表紙画像の埋め込み自体は解消済みだが、実機（あるいはビューア）によって表紙が一回り小さく表示されるケースがある。
* **原因:** EPUBカバーの想定アスペクト比（1600×2560, 比 1:1.6）と、本文のA4サイズ（比 1:1.414）の比率差による見え方の違い。カバー画像側の設計上の仕様とも言える。
* **ステータス:** 軽微。実機での確認を進め、実害がないと判断できればこのままチケットをクローズ（完了）とする。
