# Status（仕様書の実装進捗）

> 💡 **運用のルール**
> - 実装が完了したら本ファイルから該当行を削除し、仕様書ファイルは `git mv docs/specs/xxx.md docs/archives/` で移動する（`docs/specs/archives/` ではなく `docs/archives/` が既存の置き場所）。
> - まだ仕様化していないアイデア段階のものは本ファイルではなく `PLANNED.md` に置く。仕様書を書いた時点で本ファイルへ移す。
> - 知見メモ（`*-notes.md`）とガイドラインは本ファイルではなく `NOTES.md` の索引に置く。`docs/specs/` に足したら、あちらへ 1 行足す。
> - 状態が変わったら都度その場で更新する。放置すると `PLANNED.md` に実装済み項目が残り続ける事故が起きる（2026-07-08 に実例あり: `terminal-literal-spec` 完了後も未完了項目として残っていた）。
> - 「メモ（依存関係・実装順序）」に書くのは**実装待ちの仕様書どうしの依存**（「A を先に入れると B が楽」等）だけ。**完了報告は書かない**——それは `CHANGELOG.md` の役目で、両方に書くと必ず片方が stale になる。実装して分かった落とし穴は**その仕様書自身の「実装記録」節**へ書いてから `docs/archives/` へ送る。やり残した作業は `PLANNED.md`（将来対応）か `KNOWN_ISSUES.md`（未解消の不具合・制限）へ移す。

---

## 一覧

| 仕様書 | 内容 | 状態 |
| :--- | :--- | :--- |
| `build-mode-parity-spec.md` | 単章ビルドと全章ビルドで章本文の結果が食い違う。ステップ表がモードごとに手で維持されているため差分が増え続ける。相（`:shared`/`:pdf`）と直交する第 2 の軸「単位（`unit: :chapter` / `:whole_book`）」を導入し、単章モードを導出に置き換える | 実装待ち |
| `index-markup-plain-fallback-spec.md` | 著者が明示した `[用語]` が索引語にならず、`[<h1>]` が本物の見出しになる不具合。前処理の `strip_index_markup!` が索引スキャナより先に括弧を外していることと、素のテキスト化でエスケープしないことの 2 点 | 実装待ち |

---

## 参考メモ

知見メモ・ガイドライン・検討メモの索引は `NOTES.md` に移しました（本ファイルは「実装済みか？」に専念します）。

---

## メモ（依存関係・実装順序）

**`build-mode-parity-spec.md` を先に入れると、`index-markup-plain-fallback-spec.md` の §4.2（述語の新設）は不要になる。** 単章でも索引タグ付けが走るため、`strip_index_markup!` という代替そのものが消えるからである。順序を逆にすると、述語を作ってから撤去することになる。

ただし索引側の不具合（`[<h1>]` が本物の見出しになり目次を汚染する）は**現に紙面を壊している**ため、リリース時期との兼ね合いで先に止める余地はある。その場合も §4.1（`IndexMarkup.plain_text` によるエスケープの正典化）はパリティ仕様と独立して価値があり、捨てずに残る。詳細は `build-mode-parity-spec.md` §8。

どちらも検証には記法網羅フィクスチャ（`test/vivlio_starter/fixtures/notation/`）を使う——`04-index-markup.md` は**現行版で 4 件を検出済み**なので、修正が効いたかどうかは赤→緑で判定できる。
