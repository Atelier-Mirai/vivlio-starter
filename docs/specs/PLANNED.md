# Planned（将来計画）

本システムの将来対応・改善アイデアを目的別に整理する。

- 見出しは目的別の `##`／`###` に統一し、各項目は `-` の箇条書きで記す。
- 優先度は行頭に `[High]` / `[Medium]` / `[Low]` で付す（未判定のものは付けない）。
- 既存仕様書がある項目は末尾にファイル名を添える（`docs/specs/` / `docs/archives/` は付けない。実装後に移動しても壊れないようにするため）。

---

## Version 2.0 構想（メジャーアップデート）

後方互換を破る大きな方向転換を伴うため、v2.0 でまとめて取り組む。

- [High] **小説（挿絵入り）への対応**: 現在は技術書専用。小説向けレイアウト（縦書き・挿絵配置・章扉など）に対応できるようテーマ／組版を拡張する。
- [High] **テーマシステムの実装**: vivliostyle 公式の `bunko.css` などの既存テーマ CSS を活用。`book.yml` からのテーマ選択、CSS の動的切り替え、小説用縦書き・技術書用横書きのプリセット提供。
- [Low] **CLI 終了コードの体系化**: 現状「正常 0 / 問題あり 1」の 2 値を、UNIX 慣例（例: grep の 0=見つかった / 1=見つからない / 2=エラー）に倣いエラー種別ごとに分ける。影響範囲が広く後方互換を破るため v2.0 で対応。

---

## ビルド / 出力

- [Low] **画像の width 属性自動補完**: `![](foo.png)` のように幅指定なしでも、実寸やクラス指定に応じて `width=100%` 等を自動補う（大判図をページ送りにせず収めるため）。
- [Medium] **「表1＋背＋表4 を 1 枚にくるみ表紙」の形**: 入稿先の慣習でくるみ表紙を求められる場合も有るが、結合処理を伴う別機能になるため、現状は非対応。

- [Low] **版面の分断を機械が見つける**: `page.section_pagebreak: false` を選ぶと、見出しやキャプションがページ末尾に取り残される事故が起こりうる。節が必ずページ先頭から始まっていた頃は**原理的に起きなかった**ので、防御が無かった。**判明していた 2 種は CSS で塞いだ**（2026-08-10）ので、残るのは「塞ぎ漏れを見つける」仕組みの話。
  - **見出しの孤立は PDF では起きない**。`break-after: avoid` を足したが、**PDF ではこの宣言の有無で結果が変わらない**——Vivliostyle が既に見出しを次の内容と一緒に送るためで、「見出しは収まるが続く 1 行が収まらない」高さを B5・spacer 176〜196mm の 1mm 刻みで探しても孤立する組み合わせが無かった。宣言は EPUB / Kindle 向けの保険として残してある。
  - **キャプションの孤立は起きた**。同じ条件でも `<p class="table-caption">` / `.code-caption` は後続の表・コードと一緒に送られない。A/B 実測で **規則なし 8/14・規則あり 0/14**、本書でも実在の分断が 2 件あった（リスト 6-1・6-2）。`break-after: avoid` で解消（`table.css`）。Re:VIEW Starter の `caption_needspace` 相当。
  - **検出は描画結果に対して行う**。`pdftotext` の抽出順は視覚順と一致せず**単独では使えない**——本書 449 ページを「ページ末尾の行が見出し／キャプションか」で走査すると、見出しで 2 件・キャプションで 3 件挙がったが、**見出しは 2 件とも、キャプションは 1 件が誤検出**だった（後ろに内容があるのに抽出順の都合で最後に来ていた）。Vivliostyle の組版結果か PDF のテキスト描画座標を併用する必要がある。
  - **原稿側の「取り残した改ページ」は 2026-08-16 に実装済み**（`Guards::StrayPageBreakCheck`・`vs preflight` の 🟡）。描画結果を見る必要がなく原稿だけで判定できるため、残る 2 種より先に片づいた。**判型を変えた直後こそ走らせたい**——版面が変わると「たまたま目立たなかった」箇所が浮かぶ（この項目自体、B5 → A4 の変更で 4 件が露見して立った）。
  - 出し方はビルド後の 🟡 か `vs preflight` の一部。指摘には**ページ番号と見出し／キャプション名**を添え、`@pagebreak` で個別に逃がす道を示す。
  - 関連: `page-break-control-spec.md`（`---`＋`##` の二重改ページ正規化）・`chapter-pagebreak-spec.md`

---

## 記法・置換ルール


- **`@` ディレクティブの拡張**: Tier 2（`@nobr`・`@fill`・`@index`）と `@abbr` の代替案（用語集統合 or 標準 `*[…]:` `<abbr>`）はブレスト段階 → `at-directive-ideas.md`

- [Medium] **目次ラベルが `@` ディレクティブを解釈してしまう**: 見出しの中でインラインコードとして書いた記法（`` ### 改ページ `@pagebreak` ``）は、本文では `<code>` に守られて無傷だが、**目次ラベルはバッククォートの落ちた素のテキスト**（`data-heading` 由来）なので、そこへ置換ルールが当たる。実測: 本書の目次で `改ページ <div class="vs-break-page"></div>` が生まれていた（`<span>` の中に `<div>` が入る不正な入れ子でもある）。いまは実害が出ていない——改ページ記法の CSS を生成ページへ配っていないため（`pagebreak.css` のコメント参照）。ただし**記法を解説する本では踏みやすい形**なので、いずれ塞ぐ。
  - 直し方の候補: (1) 目次ラベルを `data-heading` の素テキストでなく見出しの内側 HTML（`<code>` を保った形）から作る、(2) 生成ページ（`_toc` ほか）を `@` ディレクティブ置換の対象から外す。(1) のほうが「記法は原稿にだけ効く」という筋に合うが、索引タグ（`.index-term`）など見出し内の他の装飾をどう落とすかの整理が要る。

- [Medium] **`vs furigana`（半自動ルビ付与）**: 小学生向けなど、対象読者の学年より上の漢字へ振り仮名 `{漢字|よみ}` を**半自動**で付けるコマンド。`vs metrics` の「漢字レベル（ルビ候補）」で**どの漢字が対象か**は把握できるので、その先の「本文へ実際にルビ記法を書き込む」変換を担う。
  - **対象の指定**: `book.yml` や引数で「基準レベル」を選ぶ（例: 小4向け＝小5以上／中学以上／常用外のみ、等。レベル定義は `furigana-level-spec.md` 参照＝`vs metrics` と共通の L0〜L4）。
  - **読みの生成と限界（最重要）**: 読みは MeCab から得るが、**まさにルビを振りたい稀な漢字（未知語）で最も外れる**（例「碍子」は `碍` が非常用で未知語となり誤分割・誤読になりやすい）。よって**全自動で正確なルビは不可能**という前提に立ち、(1) 読みが不確実・未知の語にはルビを付けずに**警告＋出現箇所を提示**して著者に委ねる、(2) 付けたルビも**要確認**として一覧化する、という半自動方式にする。将来的に漢字→音訓の補助辞書でカバー率を上げる。
  - **補助辞書の種はもう手元にある**（2026-08-18）: 交ぜ書き辞書（`mazegaki_dictionary.rb`）は「かな書き → 漢字表記」の対がそのまま**表外漢字の読み**になっている。`だ円 → 楕円` の「だ」は「楕」の読みであり、共通の接頭辞・接尾辞を削るだけで機械的に取り出せる。**実測: 140 語から 139 件・ユニーク 125 字**が得られ、同じ字に別の読みが付いた衝突は 1 字だけだった（`鬱` の「うつ」と「うっ」＝促音便で実質同じ）。
    - **上の「碍子」の例がそのまま解ける。** `がい子 → 碍子`・`障がい → 障碍`・`障がい者 → 障碍者` の 3 経路すべてが `碍 → がい` で一致する。`閏 → うるう` も 3 経路一致、`閾 → しきい`・`籤 → くじ`・`爬 → は`・`冪 → べき` も取れる。**MeCab が最も外す領域と、この辞書が覆う領域が重なっている**のが要点で、常用漢字の読みは MeCab が正しく返すため補う必要がない。
    - **限界**: (1) 熟語由来なので**音読みに偏る**（訓読み・送り仮名のある語は取れない）、(2) 連濁が混じる（`子ども → 子供` から `供 → ども` が出るが、字の読みは「とも」。**語単位の読みとしては正しい**ので、字へ分解せず語のまま使うほうが安全）、(3) 語数は辞書の規模なりで、ルビが要る語全体から見れば小さい。
    - 抽出ロジックは本体に置いていない（使う側が無いうちに作っても腐るため）。`vs furigana` に着手するときに `MazegakiDictionary.readings` として起こす。`KANJI_CHOICE`（`障害者 → 障碍者`）は漢字どうしの対応なので、かな側が読みでないことを見て自動で外れる。
  - **安全性・冪等性**: ファイルを改変するため、(1) 既に `{…|…}` でルビ済みの箇所を二重化しない、(2) コードブロック・URL・既存ルビ内は対象外、(3) `--dry-run` で差分プレビュー、(4) バックアップ or Git 前提。`vs metrics` は読み取り専用の統計に徹し、書き込み系はこの `vs furigana` に分離する。

- [Low] **交ぜ書き辞書を prh 形式で外部公開する（1.0 の後）**: `mazegaki_dictionary.rb` の 150 語は、調べた範囲で**既存のどこにも無いデータ**である（RubyGems に交ぜ書き検出の gem は無く、npm の `mazegaki` は 0 件、textlint の日本語ルールは `ja-hiragana-*` のように**すべて逆方向**、prh 公式辞書 3155 行にも表外漢字を `expected` にする規則は 1 つも無い。2026-08-18 実測）。公開すれば textlint 利用者がそのまま使える。
  - **形態は prh の YAML が最有力。** データだけで済み実装コストがゼロで、利用者は `rulePaths` に 1 行足すだけで使える。制約つきの語（`(?<!ん)だ円`）も prh は正規表現を受け、後読みは Node が対応している。**Ruby gem 化は優先度が低い**——Ruby には日本語校正のエコシステムがほぼ無く（あるのは textlint ラッパー 3 つだけ）、使う土壌の無いところへ配ることになる。textlint ルール（npm）としての実装も、prh 辞書で足りるぶん重複になる。
  - **出せるのは第 1 層だけ。** 第 2 層（`lint/data/mazegaki.tsv` の 1,912 語）は形態素の境界判定が前提なので、prh の単純置換に載せると誤検出になる（`mazegaki-two-tier-spec.md`）。公開物は 150 語のままになる。
  - **二重管理を避ける形**: **正典は Ruby 側（`mazegaki_dictionary.rb`）に置き、prh YAML は生成物にする**。辞書を足すのは Ruby だけ、公開物は rake タスクで毎回吐き直す。生成は 30 行程度で書ける。逆向き（YAML を正典に Ruby が読む）にすると、gem 同梱物が増えるうえ採否の理由コメントが YAML へ移せない。
  - **振り仮名側は `vs furigana` の実装後**。いま公開できるのは「表外漢字 125 字の読み」という素材だけで、語単位か字単位か・連濁をどう扱うかは実装が決める。形が決まる前に配ると、後から互換を壊すことになる。
  - **1.0 より前に出さない理由**: 公開すると issue と PR の受け口が増え、辞書は「この語も入れて」が来やすい。採否の基準は実際に 2 度動いており（§`lint-japanese-prose-rules-spec.md` 実装記録）、`かく乱` は誤った理由で一度落として後から救っている。**この判断を外部から継続的に求められる状態**を 1.0 前に抱えるのは重い。

- [Low] **交ぜ書き語を原稿から集めて辞書を育てる**: 辞書は gem 側にあり著者が足せないため、**「この語も交ぜ書きだ」と気づいても行き先がない**。2 段化（`mazegaki-two-tier-spec.md`）で 150 → 2,062 語になったが、**語彙は実際の原稿からしか育たない**。残っているのは次の 3 案で、いずれも未着手。
  - **案 1: 候補を機械が挙げる（`vs lint --suggest` 等）。** 交ぜ書きは「かな 1〜3 字＋漢字」という**形**を持つので、MeCab で 1 形態素として認識される語のうち、かなと漢字が混在し辞書に無いものを候補にできる。**`vs index:auto` と同じ流儀**（候補抽出 → レビュー用ファイル → 著者が選ぶ）に載せられるのが利点で、新しい操作を覚えなくて済む。
    - **ノイズの絞り込みが肝。** 素朴にやると「お願い」「引き出し」「取り消し」のような**正当なかな漢字混在語**が大量に混じる。効きそうな条件は (1) 1 形態素であること、(2) かな部分が 3 字以下、(3) **漢字部分が常用漢字表外を含む**——(3) の判定は `furigana-level-spec.md` の L0〜L4（`vs metrics` と共通）が既に持っている。それでも「送り仮名を伴う動詞」は残るので、品詞が名詞のものに限るとよい。**この絞り込みは第 2 層の抽出で実証済み**（`lint/data/filter.rb` の 6 条件）なので、そのまま転用できる。
    - **著者の手元で完結する**のがこの案の強みで、報告に依存しない。本体の辞書へ還るかは別問題だが、少なくともその著者の原稿は直る。
  - **案 2: 著者辞書（`config/mazegaki_extra.yml`）。** 分野固有の語を著者が足せる窓口で、`textlint_allowlist.yml`（黙らせる）の対になる。ただし**集約の仕組みではない**——足された語が本体へ届くかは著者の報告次第である。公開後なら PR の受け口として機能する。
  - **案 3: `allowlist` に足された語を見る（逆向き）。** 「この語はかなで通す」と登録された語は、**辞書が過剰だった証拠**である。集まれば採否基準の較正に使える（`ひな型`・`かぎ括弧` は本書で実際にその候補になった）。これも著者が知らせない限り届かない。
  - **順序**: 案 1 が単独で価値を持つので先。案 2・3 は公開して受け口ができてからで足りる。
  - **外部辞書の候補は調べ尽くした。** Mozc 用の抑制辞書（`akirakubo/mazegaki`）は**不採用**、SudachiDict の正規化表記を採用して第 2 層にした。判断の理由は `mazegaki-two-tier-spec.md` の実装記録にある——**再調査しないこと。**

- [Low] **文体（敬体・常体）の混在を実際に見張る独自ルール（1.0 の後）**: textlint の `no-mix-dearu-desumasu` は名前に反して混在をほとんど拾えない。判定を担う `analyze-desumasu-dearu`（5.0.2）は、常体を**「で」＋「ある」の並びだけ**、敬体を**「です」「ます」の基本形だけ**で数える（2026-09-15 にソースで確認）。「〜だ」「〜した」「〜する」「〜のだ」も「〜ました」「〜ません」「〜でしょう」も、どちらにも数えない。本書全体にかけて拾ったのは引用文 1 件だけだった。
  - **案: ProseChecker に、文末の活用形で判定するルールを置く。** MeCab は本体の依存にすでにあるので、文末の助動詞（「です」「ます」とその活用 vs 「だ」「た」「ない」）と動詞の終止形を見て、章の多数派と違う文を指摘する。
  - **受け入れ例**: 箇条書き「設定ファイルはプロジェクト直下に置きます。」「変更は再起動後に反映される。」の組を**混在として拾う**こと（現行ルールでは素通りする）。一方、体言止めだけのリストと、本文と同じ敬体の文だけのリストは黙る。
  - **箇条書きの規則（2026-09-15 に決定）**: 独自ルールが見張る書き方は次の 3 つとする。
    1. 短い項目は**体言止め**にする（句点なし）——「インストールが簡単」「日本語の資料が豊富」
    2. 文になる項目は、**そのリスト全体を本文と同じ敬体**にする（句点あり）——「設定ファイルはプロジェクト直下に置きます。」「変更は再起動後に反映されます。」
    3. **1 項目でも体言止めにできなければ、そのリストはすべて文にそろえる**——×「高速」「メモリ消費が少ないです」／○「高速に動作します」「メモリ消費が少ないです」
    - **句点の検査もこのルールが担う。** textlint の `ja-no-mixed-period`（3.0.2）は `ListItem` を最初から除外しており（ソースで確認）、箇条書きの句点は現状どこも見ていない。体言止めなのに句点がある・文なのに句点がない、の両方を拾う。
    - **ラベル付きの段落にも同じ考え方を当てる。** 「**症状**: …」「**原因**: …」のような段落が文になっているなら、敬体＋句点にそろえる。**実装したら 43 章で検出を確かめる**——トラブルシューティングの節に、常体で句点のない「症状」「原因」の段落が 8 か所あり（2026-09-15 時点）、ルールで拾えるかを見るために手では直さずに残してある。
    - **手順の箇条書きの動詞止め（「〜をインストールする」）も指摘の対象になる。** 敬体の本でも常用される書き方だが、規則 2 により敬体（「〜をインストールします。」）か体言止め（「〜のインストール」）にそろえる。当初は誤検出の源として挙げていたが、規則を決めたことで対象へ移った。常用されている分、実装直後は指摘が多く出る見込みなので、件数を実測してから既定で有効にする
  - **誤検出の源を先に潰す**。現行ルールが「である」に絞ったのはおそらくこのためで、広げれば同じ壁に当たる。
    - 引用（`>` とカギ括弧の中）、会話文（`:::{.talk}`）
    - 体言止め、括弧書き、コード・数式
  - **現行ルールの構造（参考）**: `textlint-rule-no-mix-dearu-desumasu`（6.0.4）は、判定器を**本文・見出し・箇条書きの 3 つ別々**に持つ（`BodyMixedChecker` / `HeaderMixedChecker` / `ListMixedChecker`）。文書の要素は `Header` → 見出し、`ListItem` → 箇条書き、`Paragraph` → 本文へ振り分けられ（見出し・箇条書きの中の段落は本文に数えない）、文書の終わりに 3 つがそれぞれ**自分の集めた文だけ**で結果を出す（2026-09-15 にソースで確認）。
    - 設定も場所ごとに 3 つある（`preferInBody` / `preferInHeader` / `preferInList`）。値を与えると決め打ち（その文体でなければ指摘）、空なら自動判定（その場所の多数派と違う文を指摘）になる。`preset-ja-technical-writing` は本文＝です・ます・箇条書き＝である と決め打ちし、`preset-japanese` はオプションなし＝3 つとも自動判定で読み込む。
    - そのため**場所をまたいだ食い違いは見えない**。本文がです・ますで見出しだけ「である」でも、見出しどうしが揃っていれば指摘されない（実例: 本書の前書き 13 行目の見出しは素通りし、同じ文を本文に置いた 15 行目だけが拾われた）。
    - 本文の判定では、リンク・インラインコード・画像・引用・強調（`*…*`）の中の文字を除く。
    - 独自ルールでも、場所ごとに分けるか（箇条書きは体言止めや動詞止めが多く、本文と同じ基準では誤検出が増える）、分けたうえで場所をまたいだ食い違いも見るかを決める。
  - **判定の単位は、箇条書きならリスト 1 つ**（上の規則で決まった）。現行ルールは章の箇条書きを 1 つにまとめて判定するので、体言止めのリストと文のリストを別々に置いても混在と言われるが、独自ルールではリストごとに見る。本文と見出しの単位は引き続き決める。
  - **進め方**: 本書の原稿で候補を出し、誤検出率を実測してから既定で有効にするか決める（交ぜ書き辞書の 2 段化と同じ流儀）。自動修正はしない——敬体と常体のどちらへ揃えるかは著者が決めること。
  - **実装したら片づけるもの**: 暫定の表示文の置き換え——`TextlintFormatter::RULE_SUMMARIES` の `'no-mix-dearu-desumasu' => '「である」と「です・ます」が混在しています。'` の行を削除する。この置き換えは、判定の実態（「である」で終わる文だけ）より広く聞こえる元の文言を抑えるための暫定措置で、本物の検出が入れば役目を終える。あわせて textlint 側の `no-mix-dearu-desumasu` を `SUPERSEDED_TEXTLINT_RULES` へ入れて常に切り、二重に指摘しないようにする。

- [Low] **参考文献サポート**: 簡易 BibTeX / CSL 相当の仕組みを検討する。脚注そのものは実装済み（`footnote_converter.rb`。リンクの脚注化も前処理で入っている）ので、残るのは**文献リストと引用の対応づけ**である。
- [Low] **索引語数の目安（`BASE_TERMS`）を実測で見直す**: 現在の値は刊行済み技術書 10 冊の実測だが、**`thorough`（丁寧に索引を拾う本）は 2 冊しかない**（226・228）。幅が狭いのは冊数のせいで、その帯が本当に狭いことを意味しない。索引付きの技術書 PDF が増えたら追加計測して更新する。計測方法・生データ・落とし穴は `index-size-calibration-data.md`（§2 に精度の限界、§8 に「語彙リストより機構」の一般則）。
  - 2026-08-05 に `n月刊ラムダノートVol3No1(2021).pdf` を追加したが、**この誌に索引は無い**（本文中の「索引語」という語に当たるだけ）ので `thorough` の冊数は増えていない。章あたりの分量の較正には使えたため、そちらは `chapter-volume-calibration-data.md` §3.3.1 に記録済み。

- [Low] **本文 † の重複排除（Step 8）を撤去できるか判断する**: `BacklinkDeduplicator#deduplicate_body_glossary_links!` は「同一ページ内の 2 回目以降の †」を消す処理だが、† を章ごとの初出だけに絞った（2026-08-16）ことで**同一ページに 2 個並ぶ状況がなくなり、常に 0 件**になった。害はないが本文 HTML を Nokogiri でパースするコストは残る。**紙面の目視確認で「章ごとの初出」が確定してから**撤去すること——差し戻す可能性がある間は残しておくほうが安い。用語集ページ・索引ページ側の dedup は引き続き必要なので、消すのは本文の 1 経路だけである。

- [Medium] **本書の索引を実際に整理する**（機能の話ではなく原稿側の作業）: `vs index:auto` が一般語 16 語を `[-i]` で提示し、広く散らばる 55 語に主要参照の候補を添えるところまでは自動化された。**どれを外し、どれに主要参照を付けるかは著者の判断**で、まだ手を付けていない。`Markdown` `PDF` のように本書の主題そのものである語は、外すのではなく説明章を指定するのが筋。

- [Low] **擬人法の指摘（`personification`）**: 「黄金比が顔を出す」「再帰が顔を出す」のように、抽象概念を主語にした擬人的表現を `vs lint` で拾う。判定は「部位 × 出現動詞」の直積（`顔／姿／頭` × `出す／見せる／のぞかせる／もたげる`）と、字義どおり顔を出せる主語の許可リスト（太陽・月・芽・彼…）の組み合わせで、主語は**末尾一致**で見る（「朝の太陽が顔を出す」を通すため）。**文法的な誤りではなく文体の選択**なので、`ProseChecker` へ入れるなら既定は黙らせて `book.yml` の opt-in（`trim_long_vowel` と同じ流儀）にする → `personification-lint-research-report.md`
  - **既製データは無い**（2026-09-01 調査）。擬人法の印が付いた公開データは存在せず、隣接資料も使えない——分類語彙表（国語研）は**商用利用が要問い合わせ**で MIT の gem へ同梱できず、日本語 WordNet（NICT）はライセンスこそ通るが約 100MB あり主語の切り出しに形態素解析も要る。
  - **`mazegaki` のように外部辞書から機械抽出する道は無い。** 交ぜ書きは「表記」の問題で、SudachiDict の正規化表記に**答えが書いてあった**。擬人法は「意味と文脈」の問題で、どの辞書にも「主語が抽象名詞なら不適切」とは書かれていない。**表記の問題なら外部辞書が使える／意味の問題なら使えない**——この線引きは他の記法ルールを考えるときにも効く。
  - **述語の列挙は必ず漏れる。** 検討の最初に `顔を出す` `姿を見せる` `頭をもたげる` を並べたところ、その組み合わせである「顔を見せる」が抜けた。直積で持つこと。
  - **本書に用例は 0 件**（同日実測）。既存原稿を直すルールではなく、これから書く文章のためのものなので優先度は低い。手書きの小辞書から始め、原稿で出会った表現を足して育てる形になる。

---

## コンテンツ / テンプレート

- [Medium] **用語集テンプレートの標準添付**: よく用いる用語をまとめた用語集テンプレートを標準添付し、プロジェクト作成時または後から選択適用できるようにする。
- [Low] **テンプレ断片スニペット**: 注意 / 補足 / Tip などのコンポーネントを断片化して提供する。
- [Low] **Web アプリ連携**: `codes/` に置いた HTML/JS/CSS のサンプルを、書籍内で QR コードや URL として紹介する仕組み。PDF 生成という主用途からは外れるため優先度は低い。

---

## EPUB / Kindle

- [Low] **ポップアップ脚注（`epub:type="noteref"` / `"footnote"`）**: 参照番号をタップすると脚注が小窓で開く方式。Kindle(KFX)・Apple Books が対応しており、非対応リーダーは現状どおり本文に流し込まれるだけなので**加えるだけで壊れない**（追加であって置き換えではない）。2026-08-02 に脚注を「左罫つきの注記ブロック」として本文と切り分けたので**当面は不要**——参照の直後に読める形になっている。読者から「本文が脚注で分断される」という声が出たときに着手する。
  - **前提はすでに揃っている**（2026-08-02 に生成物で実測）。着手時に構造を作り直す必要はない。
    - `<html>` に `xmlns:epub="http://www.idpf.org/2007/ops"` が宣言済み（名前空間の追加作業は不要）
    - 参照リンク `<a id="fnref1" href="…#fn1">` の飛び先が `aside` の `id` と正確に対応している（`fn1`/`fn2`/`fn3`）
    - 段落内インライン span の `id` は `strip_inline_footnote_ids_for_epub!` が除去済みなので、`#fnN` の解決先が `aside` に一意（ポップアップの対象が曖昧にならない）
  - **作業は属性 2 つ**。`EpubBuilder#decorate_footnotes_for_epub!` が既に `aside.page-footnote-print[data-footnote-number]` を走査しているので、そこで `aside['epub:type'] = 'footnote'` を、対応する `a#fnrefN` に `epub:type="noteref"` を付ける。
  - **確認は実機で**。KFX のポップアップは epubcheck では検証できず、Kindle Previewer で開くしかない（`kindle-css-compatibility-notes.md` の実機確認の流儀に従う）。ポップアップ化すると `aside` は本文に出なくなるため、**CSS 側の注記ブロック体裁（`body.vs-epub aside.page-footnote`）を残すか外すか**も同時に判断する。

- **Kindle 実機での確認**（`image-format-per-target-spec.md` の残り）: 2026-08-17 に PDF・EPUB とも画像を大きく作り替えたので、KFX の表示を Kindle Previewer で確かめたい。素材そのものは 1 バイトも変わっていないため理屈上は影響しないが、Kindle 向けの画素数が版面基準の縮小で下がった（画面幅 1024px × 紙面で占めていた割合）。特に**版面の半分に置いた図が 493px 程度まで落ちる**ので、拡大表示したときの粗さを目視で見ておく。確認の流儀は `kindle-css-compatibility-notes.md` に従う。
- [Low] **Kindle 固定レイアウト（`kindle.layout: fixed`・PDF ラスタライズ流用）**: A5 PDF をページ画像化して固定レイアウト KPF にする案。劣化対応不要で組版忠実だが、主力端末 6〜7″ は文庫（A6）サイズで判型が合わず、フォント可変・検索・配信料（約 ¥50〜90/冊 増）を失うため**見送り**。数式・図版主体の本や文庫判型向けの第 3 ターゲットとして RC 後に再検討。調査結果・実装スケッチ → `kindle-fixed-layout-ideas.md`

---

## 品質 / テスト

- [Low] **自動検証パイプライン（CI）**: 最小サンプルでのビルド、Lint、HTML ポスト処理テストの自動実行。
- [Medium] **Kindle Previewer 実機確認の積み残し**: 実装は完了しているが実機目視だけ未実施のものが 7 件ある。**まとめて 1 回の確認で消化できる**。
  - 入れ子リスト記法（`nested-list-notation-spec` §10-12）: ul のレベル別マーカー「● ○ ・」の実体注入
  - コード行番号（`epub-code-line-numbers-spec` §4）: 1 論理行＝1 行の対応とぶら下げインデント
  - `@` ディレクティブ Tier 1（`at-directive-tier1-spec` §3-4）: `@pageref` がページ番号なしのタイトルリンクへ劣化するか・`@pagebreak:recto` が単純改ページになるか・QR コードの表示
  - キャプション・見出しの改ページ制御（2026-08-10 追加）: `.table-caption` / `.code-caption` と本文見出し（h2〜h6）に `break-after: avoid` ＋ legacy の `page-break-after: avoid` を入れた。**EPUB パッケージまで届いていることは確認済み**（同梱 `table.css` と `chapter-common.css` に宣言が残り、`<p class="table-caption">` ＋ `<table>` の並びも保たれ、epubcheck のエラー数も修正前と同じ 83 件）。未確認なのは**リーダーが実際に守るか**——ページを割るのは読み手側のエンジンなので、キャプションと表・コードが分かれて表示されないかを見る。PDF では A/B で分断 8/14 → 0/14 と実証済み。
  - 数式（`plain-math-notation-spec` §3-2・2026-08-21 追加）: インライン数式が**本文テキスト**として組まれ、読者の文字サイズ拡大に追従するか。ディスプレイ数式が PNG のまま崩れずに出るか。日本語を含む数式（`ハッシュ値 mod サーバー台数`）の書体が本文と揃うか。生成物の側は検査済み（KPF の XHTML と PNG を実測）なので、見るのは端末での見え方だけ。
  - タスクリストのチェックボックス（`markdown-notation-collision-spec` §4・2026-08-23 追加）: PDF / EPUB は罫線で枠を描き、済みは鉤つきの塗りつぶし。KFX は `appearance` を解さないため `EpubBuilder` が `□` / `■` の**文字**へ差し替える。見るのは落差が許容できるか——枠の大きさが本文の文字と揃うか、行頭が本文とずれないか。同じリストに印の無い項目を混ぜたときの見え方も確認する（`list-style: none` が行頭記号を奪う）
  - 索引の主要参照（`index-main-reference-spec` §5.3）: `.main-ref` の見た目が KFX で効くか。`font-weight: bold` に `font-family`（ゴシック）・`font-size: 1.06em`・`letter-spacing`・負の `margin-right` を重ねた（2026-08-05）。いずれもリテラル値で `var()` を含まないため理屈上は通るはずだが未確認で、とくに**フォント名は端末側で無視されうる**——ゴシックにならない場合、目印は太字だけになる。あわせて、EPUB / Kindle の索引で**ページ番号のカンマ区切りが従来どおり出ている**ことも見る（PDF 側でだけ `dd.resolved` へ切り替えたため、CSS のフォールバックが効いていないと番号が区切りなしで並ぶ）

### 堅牢性テスト（追加候補）

- [Medium] **11-3 巨大 YAML anchor の Billion Laughs 評価**: `aliases: true` 下でも Psych 5.x の制限で実害なしだが、上限値・挙動の明示的な検証余地あり。
- [Medium] **11-4 PDF 結合時の例外で中間 PDF を残す**: 結合例外時に中間 PDF を事後調査用に保持（`pdf_merger.rb` の例外ハンドリング強化）。
- [Medium] **12-2 / 12-3 冪等性・キャッシュ回帰**: 同一入力で複数回ビルドしても成果物が変化しないことの検証。
- [Medium] **`vivlio-starter-pdf` の堅牢性テスト整備**: 本体と同等の堅牢性テストをプラグインにも適用する。

---

## 開発者体験 / CLI UX

- [Low] **Re:VIEW 本体で書いたプロジェクトの取り込み（要望が出たら）**: `vs import` が対応しているのは **Re:VIEW Starter の記法**で、本体だけで書いたプロジェクトは動作を確かめていない。2026-09-13 に Re:VIEW 2.5.0 の `compiler.rb` と突き合わせた結果は「命令の集合は Starter ⊃ 本体（本体の 101 種はすべて Starter が持つ）」だが、**行頭の `-` の意味が違う**——Starter では番号つきリスト、本体では段落。未知の命令ではないので 🔴 も出ず、黙って変わる。`//list` `//emlist` のように**引数の数やオプションが本体と Starter で異なる命令**もある。

  調べ直すときの出発点は 3 つ。(1) 公式リポジトリ `github.com/kmuto/review` の `lib/review/compiler.rb`——`defblock` / `defsingle` / `definline` が並んでおり、**今回 Starter に対して使ったのと同じ方法で機械的に全記法を取り出せる**。(2) 同リポジトリ `doc/format.ja.md`（フォーマットガイド）——人間向けの記法リファレンス。(3) `NEWS.ja.md` / `CHANGELOG`——バージョン間で何が増減したか。最新の gem を入れて (1) を流せば、対応表の差分はすぐ出る。

  **(1) から取れるもの・取れないもの。** 宣言には引数の個数まで入っている（`defblock :list, 2..3` / `defsingle :vspace, 2`）ので、**命令名と引数の数の差分は機械的に出る**。取れないのは**引数の中身の意味**で、`//list` の第 3 引数が `file=パス,開始行` なのか裸の開始行番号なのかは宣言に現れない。そこは (2) の記法リファレンスと実原稿の突き合わせになる——今回 Starter に対してやったのと同じ手順で、`re-direct-import-spec.md` §1.2 の「引数の形の実測」がその成果物にあたる。

  抽出はこの 1 行で足りる（Starter と本体の両方に同じものを流して集合演算する）。

  ```ruby
  text.scan(/(?:^\s*|Compiler\.)(defblock|defsingle|definline)\s+:(\w+)/)
  ```

  判別自体は難しくない。`config-starter.yml` の有無で Starter かどうかが分かるので、行頭 `-` の扱いだけ出自で切り替えれば実用になる見込み。対応表（`re_renderer.rb` / `re_inline.rb`）は記法名で引く形なので、**差分ぶんの行を足すだけで済む**。

- [Low] **Linux / Windows の自動セットアップ対応（やるかもしれない枠）**: 現状 `vs doctor --fix` の自動インストールは macOS + Homebrew のみで、Linux / Windows は動作検証もできていない。将来的に Linux（apt / dnf など）や Windows（winget / Scoop / Chocolatey など）でも `vs doctor --fix` でひと通り揃うようにできると望ましい。需要と検証コスト次第で、対応するかどうかも含めて将来検討する。
- [Medium] **肥大化した `book.yml` を切り出す**: 671 行あり、著者が「必ず設定する項目」（`book` 22 行・`project` 7 行）へ辿り着くまでに機能別の詳細設定を延々とスクロールすることになる。内訳は `metrics` 162 行・`output` 96 行・`theme` 55 行・`typography` 47 行・`index` 44 行・`lint` 36 行で、**上位 2 節だけで 4 割**を占める。`page_presets.yml` `catalog.yml` `textlint_*.yml` のように**機能別の設定ファイルへ分ける**——どれを切り出すか（`metrics` / `lint` / `spellcheck` / `index` 系）と、`book.yml` に残す境界を決める。
  - **不要なキー・ほとんど使われないキーを先に洗う**。切り出す前に減らすほうが、分けたあとの各ファイルが小さく収まる。`dead-code-candidates-report.md` がメソッドに対してやったことの設定キー版で、**「定義はあるがコードが読んでいないキー」**（`chapter.ideal` が 2026-08-05 まで死蔵だった実例がある）と**「読んではいるが著者がまず触らないキー」**を分けて数える。
  - **後方互換に注意**。既存プロジェクトの `book.yml` からキーが消えることになるため、`vs upgrade` での移行か、両方の置き場所を読む移行期間が要る。
- [Medium] **ビルドログ整備**: 各ステップに要約出力とエラーヒントを追加し、失敗時の原因特定とリカバリーを容易にする。
- [Low] **スタイルガイド整備**: 章タイプ別（preface / chapter / appendix / postface）の設計指針、ユーティリティクラス（`.aki`, `.aki2` ほか）一覧と使用例をドキュメント化する。

---

## 後日調査

- **Kindle 表紙（KDP 渡し）** の扱い。
