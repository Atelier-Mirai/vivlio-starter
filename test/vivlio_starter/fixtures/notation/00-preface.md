# はじめに

前書きは `preface.css` が当たり、本文とは別の装飾になります。**置き場所ごとに当たる CSS が違う**ため、同じ記法でも前書き・本文・付録・後書きで結果が変わることがあります。

## 前書きの h2

前書きの h2 です。

### 前書きの h3

**この見出しが 2026-08-11 の事故の現場です。** `preface.css` の `h3::before` が飾りを `content: "📚 "` と CSS 文字列で描いており、絵文字が Type 3 フォントとして PDF に埋め込まれていました。

本書の前書きには h3 が 1 つも無かったため、全章ビルドの Type 3 ラチェット（上限 0 件）を素通りしていました。**ここに h3 が 1 つあるだけで、その死角は塞がります。**

#### 前書きの h4

前書きの h4 です。

## 前書きの中の記法

前書きでも本文と同じ記法が使えることを見ます。

:::{.note}
前書きの中のコンテナです。
:::

> 前書きの中の引用です。
>
> ```ruby
> puts "前書きの中の、引用の中のコード"
> ```

索引語 [前書き索引|まえがきさくいん] と脚注^[前書きの中の脚注です。] を置きます。
