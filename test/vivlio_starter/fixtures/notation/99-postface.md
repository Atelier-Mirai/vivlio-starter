# 終わりに

後書きは `postface.css` が当たります。`postface.css` は `preface.css` を `@import` しているため、**前書きの装飾をそのまま引き継ぎます**。📚 の事故が前書きと後書きの両方に及んでいたのはこのためです。

## 後書きの h2

### 後書きの h3

前書きと同じく、ここに h3 があることで `preface.css` 由来の `h3::before` が後書きでも発火します。

## 後書きの中の記法

:::{.note}
後書きの中のコンテナです。
:::

> 後書きの中の引用です。
>
> ```ruby
> puts "後書きの中の、引用の中のコード"
> ```

索引語 [後書き索引|あとがきさくいん] と脚注^[後書きの中の脚注です。] を置きます。
